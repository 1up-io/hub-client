{
    "name": "1up-io/hub-client",
    "description": "Hub Client",
    "license": "proprietary",
    "type": "project",
    "require": {
        "php": "^8.4",
        "symfony/console": "^7.4",
        "symfony/dependency-injection": "^7.4",
        "symfony/config": "^7.4",
        "symfony/yaml": "^7.4",
        "symfony/process": "^7.4",
        "siketyan/yarn-lock": "^1.1",
        "symfony/http-client": "^7.4",
        "phpseclib/phpseclib": "^3.0",
        "symfony/dotenv": "^7.4"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "bamarni/composer-bin-plugin": "^1.8",

        "symfony/var-dumper": "^7.4"
    },
    "bin": ["bin/hub-client"],
    "extra": {
        "bamarni-bin": {
            "forward-command": true,
            "bin-links": false,
            "target-directory": "tools"
        }
    },
    "config": {
        "allow-plugins": {
            "bamarni/composer-bin-plugin": true
        }
    },
    "scripts": {
        "pipeline": [
            "@ecs",
            "@phpstan",
            "@dependency-analyser"
        ],
        "dependency-analyser": [
            "@php tools/composer-dependency-analyser/vendor/bin/composer-dependency-analyser"
        ],
        "fix": [
            "@php tools/ecs/vendor/bin/ecs check src/ tests/ --fix --ansi"
        ],
        "ecs": "@php tools/ecs/vendor/bin/ecs check src/ tests/ --ansi",
        "phpstan": "@php tools/phpstan/vendor/bin/phpstan analyze src/ --level 9 --ansi --memory-limit=-1 -c phpstan.neon"
    }
}
